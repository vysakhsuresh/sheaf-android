<?xml version="1.0" encoding="utf-8"?>
<!--
  The index holds text read out of the user's screenshots - bank balances, chats, IDs. It is
  derived data that Deja can always rebuild from the screenshots themselves, so there is no
  reason to let it leave the device in a cloud backup or a device-to-device transfer.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </device-transfer>
</data-extraction-rules>
